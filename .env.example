# Local-only defaults. Copy this file to .env and change values if local ports conflict.
POSTGRES_DB=loan_platform
POSTGRES_USER=loan_platform
POSTGRES_PASSWORD=devbank_local_only
POSTGRES_PORT=5432
KAFKA_PORT=9092
API_PORT=8080
SERVER_PORT=8080
FRONTEND_PORT=3000

# Application runtime configuration
KAFKA_CONSUMER_GROUP_ID=loan-platform-processing
KAFKA_TOPIC=loan-application-events
LOAN_PLATFORM_DEMO_DATA_ENABLED=true
BACKEND_URL=http://loan-api:8080
VITE_BACKEND_URL=http://localhost:8080
